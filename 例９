#include "string.h"
#include "stdio.h"
#include "stdlib.h"
int main ()
{ int i,j,sum;
  char a=79,b=88;
   for(i=1;i<9;i++)
{      for(j=1;j<9;j++)
   {    sum=i+j;
       if(sum%2==0) printf("%c",a);
       else printf("%c",b);}
printf("\n");}
}
