#include <math.h>
#include "string.h"
#include "stdio.h"
#include "stdlib.h"

 main ()
{
 int i,j,a=0,b;
  for (i=101;i<=200;i++)
{      
 for (j=2;j<=sqrt(i);j++)
  { 
      if(i%j==0) 
           a++;
        else a+=a;
  }
    if(a==0)  
     printf("%d\n",i);
     else printf("error\n");
     a=0;
   }
}
